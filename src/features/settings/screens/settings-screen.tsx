import { FeaturePlaceholder } from '@/features/foundation/components/feature-placeholder';

export function SettingsScreen() {
  return (
    <FeaturePlaceholder
      description="Personalize how Planora looks and behaves, with accessible defaults from the start."
      emptyDescription="Preferences, data controls, and account options will be added only when their supporting features exist."
      emptyTitle="Your preferences will live here"
      icon="options-outline"
      title="Settings"
    />
  );
}
